- hosts: all
  become: true
  vars_files:
    - "{{ playbook_dir }}/../../secrets/vault.yml"

  roles:
    - common
    - docker
    - xray

  tasks:
    - name: Configure nginx reverse proxy (optional)
      include_role:
        name: nginx
      when: xray_manage_nginx | default(true) | bool
